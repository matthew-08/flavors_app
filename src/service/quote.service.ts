import { PostQuoteInput } from '../schema/quote.schema'

const postQuoteService = ({
    capability,
    comments,
    email,
    name,
    newsletter,
}: PostQuoteInput) => {}

export { postQuoteService }
